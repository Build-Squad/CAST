// This transaction will need the community store address along with the proposal ID
// to cast a vote on that proposal

// actually this could check if you have already joined the community and if not
// do what join_community tx does